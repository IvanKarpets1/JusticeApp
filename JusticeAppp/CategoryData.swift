import Foundation
import UIKit
class Category {
    
    init(image: UIImage, type: String) {
        self.image = image
        self.type = type
    }
    
    let image: UIImage!
    let type: String!
}
